/*
 * Project: {{ project_name }}
 * Generated by KiFlow AI - Level 3 Autonomous & Realistic
 * Description: {{ description }}
 */

{% for mapping in pin_mappings %}
const int {{ mapping.name }} = {{ mapping.pin }}; // Connected to {{ mapping.comp_id }} pad {{ mapping.pad }}
{% endfor %}

void setup() {
  Serial.begin(115200);
  Serial.println("{{ project_name }} - Firmware Initialized");

  {% for mapping in pin_mappings %}
  {% if "LED" in mapping.name or "OUTPUT" in mapping.name.upper() %}
  pinMode({{ mapping.name }}, OUTPUT);
  {% elif "BUTTON" in mapping.name or "INPUT" in mapping.name.upper() %}
  pinMode({{ mapping.name }}, INPUT_PULLUP);
  {% endif %}
  {% endfor %}
}

void loop() {
  // Add your logic here
  delay(100);
}
